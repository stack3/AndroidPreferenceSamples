<?xml version="1.0" encoding="utf-8"?>
<manifest package="net.stack3.preferencesamples"
    android:versionCode="1"
    android:versionName="1.0" xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="net.stack3.preferencesamples.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".programatic.ProgramaticSampleActivity"></activity>
        <activity android:name=".preferencescreen.PreferenceScreenSampleActivity"></activity>
        <activity android:name=".preferencescreen.ReadDefaultSharedPreferencesActivity"></activity>
    </application>

</manifest>
